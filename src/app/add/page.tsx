"use client";

import RoundForm from "@/components/RoundForm";

export default function AddPage() {
  return (
    <main className="page">
      <RoundForm title="라운드 수동 추가" submitLabel="라운드 저장" />
    </main>
  );
}
